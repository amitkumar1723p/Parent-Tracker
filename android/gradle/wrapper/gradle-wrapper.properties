distributionBase=GRADLE_USER_HOME
distributionPath=wrapper/dists
distributionUrl=https\://services.gradle.org/distributions/gradle-8.14.3-bin.zip
networkTimeout=10000
validateDistributionUrl=true
zipStoreBase=GRADLE_USER_HOME
zipStorePath=wrapper/dists
hermesEnabled=true



# # =========================
# # Gradle properties for production (android/gradle.properties)
# # =========================

# # Enable Hermes (must be true for builds to include Hermes)
# hermesEnabled=true

# # Recommended: increase Gradle JVM memory for faster builds (use sensible value for CI/your machine)
# org.gradle.jvmargs=-Xmx4096m -Dfile.encoding=UTF-8

# # Improve dexing / build speed for large projects (helps with multidex & large dependencies)
# android.enableDexingArtifactTransform=false

# # Ensure AndroidX and Jetifier enabled (most RN projects use these)
# android.useAndroidX=true
# android.enableJetifier=true

# # ---------------------------
# # PRODUCTION-ONLY NOTES:
# # - Keep hermesEnabled=true for production.
# # - If you need to debug with Chrome (dev), you can temporarily set hermesEnabled=false
# #   and rebuild. But production builds should keep Hermes enabled.
# # ---------------------------
